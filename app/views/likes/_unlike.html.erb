<%= form_for(post.likes.find_by(user_id: current_user.id), html: { method: :delete }) do |f| %>
	<%= button_tag(type: 'submit', class: "btn btn-default") do %>
	 <i class="glyphicon glyphicon-thumbs-down"></i> Unlike
	<% end %>
<% end %>